const conn = new jsforce.Connection({ … });

const eventData = {
  Description__c='Thanks for connecting to our application.Order has been received'
};

conn.sobject('Order_Fulfillment_Event__e').create(eventData, (err, res) => {
  if (err) {
      console.error(err);
  } else {
      console.log("Event published");
  }
});
